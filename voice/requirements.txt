# Bedrock voice layer — Python deps (install into .venv on python@3.12)
mlx-whisper>=0.4.0      # Apple-Silicon STT (Metal)
kokoro>=0.9.4           # local TTS (82M, Apache-2)
soundfile>=0.12         # wav read/write
sounddevice>=0.4.6      # mic capture
numpy>=1.26
rumps>=0.4.0            # macOS menubar app
pynput>=1.7.6           # global hotkey listener
python-dotenv>=1.0      # load .env (optional overrides)

# LLM cleanup/tighten uses the `claude` or `codex` CLI (subscription auth, no key).
# For the offline `local` backend only, also: pip install "mlx-lm>=0.19.0"
