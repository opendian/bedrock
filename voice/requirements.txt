# Bedrock Voice — Python pipeline dependencies (install via install.sh into .venv on Python 3.12)

# TTS — local Kokoro 82M, Apache-2.
kokoro>=0.9.4
soundfile>=0.12         # WAV I/O
numpy>=1.26

# Default LLM backend — fully offline on Apple Silicon, no auth.
mlx-lm>=0.19.0

# Optional: dictation daemon dependencies. Comment out the block below if you
# only use the read-aloud pipeline (most users) — saves ~150 MB on disk.
mlx-whisper>=0.4.0      # Apple-Silicon STT
sounddevice>=0.4.6      # mic capture
rumps>=0.4.0            # macOS menubar app
pynput>=1.7.6           # global hotkey listener

# Quality-of-life
python-dotenv>=1.0      # .env overrides for the constants in config.py
